<!--
Keep PRs small and consistent with the project's conventions (see AGENTS.md).
-->

## What changed

<!-- One or two sentences. If publishing a report, say which company. -->

## Checklist

- [ ] Ran `npm run verify` locally and it passed (format, lint, token check,
      types, tests, build, link check) — or the PR's CI check is green.
- [ ] No hard-coded colours/fonts/spacing added (used a token from
      `src/styles/tokens.css`).
- [ ] If a report or schema field changed, `npm run check` still passes and any
      flagged reports are fixed.
